<div class="row g-5">
  {% for row in rows %}
    <div class="col-lg-6" data-aos="zoom-in" data-aos-delay="{{ loop.index * 100 }}">
      <div class="specialty-card">
        {{ row.content }}
      </div>
    </div>
  {% endfor %}
</div>